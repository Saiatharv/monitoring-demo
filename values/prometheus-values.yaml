apiVersion: v1
kind: ConfigMap
metadata:
  name: prometheus-values
data:
  values.yaml: |
   

    prometheus:
      service:
        type: NodePort
        nodePort: 32001

    alertmanager:
      service:
        type: NodePort
        nodePort: 32004

