apiVersion: v1
kind: Service
metadata:
  name: sample-app1
  namespace: flux-system
  labels:
    app: sample-app1
spec:
  selector:
    app: sample-app1
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  type: ClusterIP
