apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: opensearch
  namespace: default
spec:
  interval: 5m
  chart:
    spec:
      chart: opensearch
      version: "2.18.0"
      sourceRef:
        kind: HelmRepository
        name: opensearch
        namespace: flux-system
  valuesFrom:
    - kind: ConfigMap
      name: opensearch-values
